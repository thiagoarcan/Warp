---
phase: 01-baseline-de-qualidade-e-seguranca-de-mudanca
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform_base/scripts/capture_runtime_baseline.py
  - platform_base/scripts/validate_all.py
  - platform_base/docs/reports/performance_baseline.json
  - platform_base/docs/reports/performance_baseline.md
autonomous: true
requirements:
  - RELY-02
  - PERF-03
must_haves:
  truths:
    - The team can run a current-state regression gate focused on critical-path behaviors.
    - The team can capture comparable startup and memory baseline numbers before later migration waves.
  artifacts:
    - path: platform_base/scripts/capture_runtime_baseline.py
      provides: runtime startup and memory sampling harness
    - path: platform_base/scripts/validate_all.py
      provides: critical-path regression gate for the current suite
    - path: platform_base/docs/reports/performance_baseline.json
      provides: machine-readable startup and memory baseline
    - path: platform_base/docs/reports/performance_baseline.md
      provides: human-readable interpretation of baseline values
  key_links:
    - from: platform_base/scripts/capture_runtime_baseline.py
      to: platform_base/launch_app.py
      via: subprocess launcher execution
      pattern: launch_app.py
    - from: platform_base/scripts/validate_all.py
      to: platform_base/tests/automated
      via: pytest critical-path invocation
      pattern: test_01_ui_loading.py
---

<objective>
Create a regression gate and runtime-performance baseline for future migration waves.

Purpose: Satisfy RELY-02 and PERF-03 by ensuring critical-path validation exists and startup/memory numbers are captured in a comparable format.
Output: A dedicated runtime baseline script, an updated regression gate script, and persisted baseline reports.
</objective>

<execution_context>
@c:/Users/tdyb/.copilot/get-shit-done/workflows/execute-plan.md
@c:/Users/tdyb/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-baseline-de-qualidade-e-seguranca-de-mudanca/01-RESEARCH.md
@platform_base/scripts/validate_all.py
@platform_base/scripts/validate_performance.py
@platform_base/launch_app.py
@platform_base/tests/automated/test_01_ui_loading.py
@platform_base/tests/automated/test_03_navigation.py
@platform_base/tests/automated/test_04_signals_slots.py
@platform_base/tests/automated/test_05_initialization.py
@platform_base/tests/automated/test_09_exceptions_errors.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a runtime baseline harness for startup time and RSS memory</name>
  <files>platform_base/scripts/capture_runtime_baseline.py, platform_base/docs/reports/performance_baseline.json, platform_base/docs/reports/performance_baseline.md</files>
  <read_first>
    - platform_base/launch_app.py
    - platform_base/scripts/validate_performance.py
    - platform_base/pyproject.toml
    - .planning/phases/01-baseline-de-qualidade-e-seguranca-de-mudanca/01-RESEARCH.md
  </read_first>
  <action>Create `platform_base/scripts/capture_runtime_baseline.py` as a standalone script that accepts the exact CLI arguments `--launcher`, `--output-json`, and `--timeout`. The script must launch `python <launcher>` as a subprocess from `platform_base/`, set `QT_QPA_PLATFORM=offscreen`, sample process RSS with `psutil`, measure elapsed startup seconds, and terminate the process cleanly when either stdout contains `Platform Base v2.0 iniciado com sucesso!` or the timeout is reached. Write a JSON artifact at `platform_base/docs/reports/performance_baseline.json` with the exact keys `launcher`, `started_at`, `startup_seconds`, `peak_rss_mb`, `exit_code`, `startup_detected`, and `timed_out`. Also write `platform_base/docs/reports/performance_baseline.md` summarizing the same values in human-readable form.</action>
  <acceptance_criteria>
    - platform_base/scripts/capture_runtime_baseline.py contains `--launcher`
    - platform_base/scripts/capture_runtime_baseline.py contains `peak_rss_mb`
    - platform_base/docs/reports/performance_baseline.json contains `"startup_seconds"`
    - platform_base/docs/reports/performance_baseline.json contains `"peak_rss_mb"`
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python scripts/capture_runtime_baseline.py --launcher launch_app.py --output-json docs/reports/performance_baseline.json --timeout 20</automated>
  </verify>
  <done>A repeatable runtime-baseline command exists and writes comparable startup and memory metrics into versioned report files.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the stale validation orchestrator with a real critical-path regression gate</name>
  <files>platform_base/scripts/validate_all.py</files>
  <read_first>
    - platform_base/scripts/validate_all.py
    - platform_base/tests/automated/test_01_ui_loading.py
    - platform_base/tests/automated/test_03_navigation.py
    - platform_base/tests/automated/test_04_signals_slots.py
    - platform_base/tests/automated/test_05_initialization.py
    - platform_base/tests/automated/test_09_exceptions_errors.py
    - .planning/codebase/TESTING.md
  </read_first>
  <action>Rewrite `platform_base/scripts/validate_all.py` so it no longer refers to historical `tests/unit` or prior phase labels. Make it run one current regression gate command with the exact pytest target list `tests/automated/test_01_ui_loading.py tests/automated/test_03_navigation.py tests/automated/test_04_signals_slots.py tests/automated/test_05_initialization.py tests/automated/test_09_exceptions_errors.py` and the exact pytest flags `-q --tb=no -x`. After the pytest command, make the script invoke `python scripts/capture_runtime_baseline.py --launcher launch_app.py --output-json docs/reports/performance_baseline.json --timeout 20`. Exit non-zero if either the critical-path pytest gate or the runtime baseline command fails.</action>
  <acceptance_criteria>
    - platform_base/scripts/validate_all.py contains `tests/automated/test_01_ui_loading.py`
    - platform_base/scripts/validate_all.py contains `tests/automated/test_09_exceptions_errors.py`
    - platform_base/scripts/validate_all.py contains `capture_runtime_baseline.py`
    - running `cd platform_base && python scripts/validate_all.py` fails when either subcommand fails
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python scripts/validate_all.py</automated>
  </verify>
  <done>The project has a current regression gate that protects critical flows and couples them with the phase baseline performance capture.</done>
</task>

</tasks>

<verification>
Run `python scripts/capture_runtime_baseline.py ...` directly once, then run `python scripts/validate_all.py` and confirm both the regression gate and baseline artifacts execute as one phase-level check.
</verification>

<success_criteria>
RELY-02 and PERF-03 are met when the project has a current critical-path validation command and machine-readable startup/memory baseline artifacts that can be compared in later waves.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-de-qualidade-e-seguranca-de-mudanca/01-02-SUMMARY.md`
</output>