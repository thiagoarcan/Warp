---
phase: 01-baseline-de-qualidade-e-seguranca-de-mudanca
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform_base/run_test_suite.py
  - platform_base/scripts/run_tests.py
  - platform_base/docs/reports/test_baseline.md
autonomous: true
requirements:
  - RELY-01
must_haves:
  truths:
    - The team can run the active automated suite from a reproducible command in any checkout.
    - Test execution leaves durable evidence with command, exit code, and pytest summary.
  artifacts:
    - path: platform_base/run_test_suite.py
      provides: portable short-form automated suite entrypoint
    - path: platform_base/scripts/run_tests.py
      provides: report-oriented canonical runner
    - path: platform_base/docs/reports/test_baseline.md
      provides: persisted baseline execution evidence
  key_links:
    - from: platform_base/run_test_suite.py
      to: platform_base/tests/automated
      via: subprocess pytest invocation
      pattern: tests/automated
    - from: platform_base/scripts/run_tests.py
      to: platform_base/docs/reports/test_baseline.md
      via: summary artifact generation
      pattern: test_baseline
---

<objective>
Establish a reproducible automated-test baseline for the active suite.

Purpose: Satisfy RELY-01 by replacing ad-hoc or machine-specific test entrypoints with stable commands and durable evidence.
Output: A portable short runner, an aligned report runner, and a checked-in baseline report file.
</objective>

<execution_context>
@c:/Users/tdyb/.copilot/get-shit-done/workflows/execute-plan.md
@c:/Users/tdyb/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-baseline-de-qualidade-e-seguranca-de-mudanca/01-RESEARCH.md
@.planning/codebase/TESTING.md
@platform_base/run_test_suite.py
@platform_base/scripts/run_tests.py
@platform_base/test_run_results.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the short automated-suite runner portable and summary-safe</name>
  <files>platform_base/run_test_suite.py</files>
  <read_first>
    - platform_base/run_test_suite.py
    - platform_base/tests/automated/conftest.py
    - .planning/codebase/TESTING.md
    - platform_base/test_run_results.txt
  </read_first>
  <action>Replace the hard-coded `os.chdir(...)` absolute Windows path with `Path(__file__).resolve().parent` so the script runs from any clone. Set `QT_QPA_PLATFORM=offscreen` and `PYTHONPATH=<project_root>/src` from that resolved root instead of fixed strings. Keep the subprocess entrypoint as `python -m pytest tests/automated`, but pass the exact arguments `--cache-clear -q --tb=no -rA` so the final pytest summary line is always emitted. Preserve non-zero exit codes and print the exact executed command, the final pytest summary line, the last 40 stdout lines, and the last 20 stderr lines.</action>
  <acceptance_criteria>
    - platform_base/run_test_suite.py contains `Path(__file__).resolve().parent`
    - platform_base/run_test_suite.py contains `tests/automated`
    - platform_base/run_test_suite.py contains `-rA`
    - running `cd platform_base && python run_test_suite.py` no longer depends on a user-specific absolute path
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python run_test_suite.py</automated>
  </verify>
  <done>The short runner executes from the repository checkout, emits a trustworthy pytest summary, and can be reused by any developer machine.</done>
</task>

<task type="auto">
  <name>Task 2: Align the report-oriented runner and persist baseline evidence</name>
  <files>platform_base/scripts/run_tests.py, platform_base/docs/reports/test_baseline.md</files>
  <read_first>
    - platform_base/scripts/run_tests.py
    - platform_base/docs/reports/
    - platform_base/pyproject.toml
    - .planning/phases/01-baseline-de-qualidade-e-seguranca-de-mudanca/01-RESEARCH.md
  </read_first>
  <action>Update `platform_base/scripts/run_tests.py` so every execution writes a plain-text or Markdown baseline artifact at `platform_base/docs/reports/test_baseline.md`. The artifact must record these exact fields: `Command:`, `Working directory:`, `QT_QPA_PLATFORM=offscreen`, `PYTHONPATH=src`, `Exit code:`, `Pytest summary:` and `Generated at:`. Keep existing HTML and JUnit report generation. Use the command actually executed by the script and capture the pytest terminal summary after the subprocess completes. If the subprocess fails, still write the artifact with the failing exit code and summary.</action>
  <acceptance_criteria>
    - platform_base/scripts/run_tests.py contains `test_baseline.md`
    - platform_base/docs/reports/test_baseline.md contains `Command:`
    - platform_base/docs/reports/test_baseline.md contains `Exit code:`
    - platform_base/docs/reports/test_baseline.md contains `Pytest summary:`
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python scripts/run_tests.py --verbose</automated>
  </verify>
  <done>The canonical runner generates the usual HTML/JUnit outputs and also leaves a durable baseline report with environment and result metadata.</done>
</task>

</tasks>

<verification>
Run the short runner and the report runner from `platform_base/`. Confirm `docs/reports/test_baseline.md` captures the final result of the report-oriented run.
</verification>

<success_criteria>
RELY-01 is met when the suite can be triggered from repository-relative commands and the result is recorded in a durable baseline artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/01-baseline-de-qualidade-e-seguranca-de-mudanca/01-01-SUMMARY.md`
</output>