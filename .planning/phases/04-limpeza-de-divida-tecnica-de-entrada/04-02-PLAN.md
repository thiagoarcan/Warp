---
phase: 04-limpeza-de-divida-tecnica-de-entrada
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform_base/test_launch.py
  - platform_base/tests/conftest.py
  - platform_base/tests/automated/test_05_initialization.py
autonomous: true
requirements: [DEDU-03]
must_haves:
  truths:
    - "test_launch.py is no longer used by automated test suite"
    - "All tests use standard pytest fixtures from conftest.py"
    - "Initialization tests exercise launch_app.py path correctly"
  artifacts:
    - path: "platform_base/tests/conftest.py"
      provides: "Pytest fixtures for application initialization"
      min_lines: 50
    - path: "platform_base/tests/automated/test_05_initialization.py"
      provides: "Test suite for app initialization via launch_app.py"
      min_lines: 40
    - path: "platform_base/_deprecated/test_launch.py"
      provides: "Archive of legacy test launcher"
      optional: true
  key_links:
    - from: "platform_base/tests/conftest.py"
      to: "platform_base/launch_app.py"
      via: "import and fixture instantiation"
      pattern: "from platform_base.ui.app import"
---

<objective>
Deprecate test_launch.py by migrating its functionality to standard pytest fixtures and ensuring initialization tests use canonical launch_app.py entry point.

Purpose: Consolidate test entry strategy; remove divergent test harness.
Output: Unified test initialization path; test_launch.py archived.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@.planning/ROADMAP.md
</execution_context>

<context>
test_launch.py was a historical test harness to validate startup in isolation.
Phase 3 established canonical module ownership; tests now use standard conftest.py fixtures.
Consolidation: Move test_launch.py logic into pytest fixtures to unify test entry.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Review conftest.py fixtures and validate they cover test_launch.py functionality</name>
  <files>
    platform_base/tests/conftest.py
    platform_base/test_launch.py
  </files>
  <action>
    Examine current test_launch.py to understand initialization flow it tests:
    - What does it import?
    - What does it validate?
    - What Qt fixtures does it set up?
    
    Compare against platform_base/tests/conftest.py to ensure all fixtures already exist:
    - qt_app fixture
    - main_window fixture
    - session_state fixture
    - Any others?
    
    If gaps exist, add missing fixtures to conftest.py.
    If test_launch.py and conftest.py are equivalent, proceed to Task 2.
    
    Document findings in commit message.
  </action>
  <verify>
    <automated>cd platform_base && python -c "import sys; sys.path.insert(0, 'tests'); from conftest import qt_app, main_window, session_state; print('Fixtures imported successfully')"</automated>
  </verify>
  <done>conftest.py validated to contain all initialization fixtures needed; equivalence confirmed with test_launch.py</done>
</task>

<task type="auto">
  <name>Task 2: Archive test_launch.py to _deprecated/</name>
  <files>
    platform_base/_deprecated/test_launch.py
  </files>
  <action>
    Move test_launch.py to _deprecated/ with deprecation header:
    
    ```powershell
    mkdir -Force "platform_base/_deprecated"
    move "platform_base/test_launch.py" "platform_base/_deprecated/test_launch.py" -Force
    ```
    
    Add deprecation notice (5 lines) at top of file:
    ```python
    # DEPRECATED: This test launcher was a historical test harness for isolated startup validation.
    # Test initialization is now handled by standard pytest fixtures in conftest.py.
    # See tests/conftest.py for current fixtures (qt_app, main_window, session_state, etc.).
    # Archived for reference only.
    #
    ```
    
    Commit with message: "chore(tests): arquivar test_launch.py historico"
  </action>
  <verify>
    <automated>test -f "platform_base/_deprecated/test_launch.py" && ! test -f "platform_base/test_launch.py"</automated>
  </verify>
  <done>test_launch.py moved to _deprecated/ with deprecation header</done>
</task>

<task type="auto">
  <name>Task 3: Validate initialization tests pass with standard fixtures</name>
  <files>
    platform_base/tests/automated/test_05_initialization.py
  </files>
  <action>
    Run initialization test suite to confirm all tests pass with canonical launch_app.py:
    
    ```
    cd platform_base
    python -m pytest tests/automated/test_05_initialization.py -q --tb=short
    ```
    
    Expected: All initialization tests pass without errors.
    These tests exercise the launch_app.py → ui.app.main() → MainWindow initialization path.
  </action>
  <verify>
    <automated>cd platform_base && python -m pytest tests/automated/test_05_initialization.py -q --tb=short</automated>
  </verify>
  <done>Initialization tests pass; test_launch.py deprecation validated</done>
</task>

</tasks>

<verification>
After tasks complete:
1. conftest.py contains all fixtures previously defined in test_launch.py
2. test_launch.py has been moved to _deprecated/
3. All initialization tests pass
4. No test regressions
</verification>

<success_criteria>
- [ ] conftest.py fixtures reviewed and complete
- [ ] test_launch.py archived to _deprecated/
- [ ] Initialization test suite passes
- [ ] no regressions in other test suites
</success_criteria>

<output>
After completion, create `.planning/phases/04-limpeza-de-divida-tecnica-de-entrada/04-02-SUMMARY.md`
</output>
