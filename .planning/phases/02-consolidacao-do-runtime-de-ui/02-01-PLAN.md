---
phase: 02-consolidacao-do-runtime-de-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform_base/run_app.py
  - platform_base/launch_app.py
  - platform_base/scripts/project_status.py
autonomous: true
requirements:
  - UICO-02
must_haves:
  truths:
    - Startup uses one canonical launcher path for normal operation.
    - Compatibility launcher no longer creates a second independent runtime path.
  artifacts:
    - path: platform_base/launch_app.py
      provides: canonical startup entrypoint
    - path: platform_base/run_app.py
      provides: compatibility wrapper delegating to canonical launcher
    - path: platform_base/scripts/project_status.py
      provides: diagnostics that report canonical startup status
  key_links:
    - from: platform_base/run_app.py
      to: platform_base/launch_app.py
      via: delegated main() call
      pattern: from launch_app import main
    - from: platform_base/scripts/project_status.py
      to: platform_base/run_app.py
      via: startup health checks
      pattern: run_app.py
---

<objective>
Consolidate startup into one canonical launcher contract.

Purpose: Satisfy UICO-02 by eliminating dual runtime entrypoint behavior and making launcher intent explicit.
Output: `launch_app.py` treated as canonical startup path, `run_app.py` converted to a compatibility wrapper.
</objective>

<execution_context>
@c:/Users/tdyb/.copilot/get-shit-done/workflows/execute-plan.md
@c:/Users/tdyb/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-consolidacao-do-runtime-de-ui/02-RESEARCH.md
@platform_base/run_app.py
@platform_base/launch_app.py
@platform_base/scripts/project_status.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert run_app.py into canonical-launcher compatibility wrapper</name>
  <files>platform_base/run_app.py</files>
  <read_first>
    - platform_base/run_app.py
    - platform_base/launch_app.py
    - platform_base/tests/automated/test_05_initialization.py
    - .planning/phases/02-consolidacao-do-runtime-de-ui/02-RESEARCH.md
  </read_first>
  <action>Replace the current `platform_base.desktop.app` import path in `run_app.py` with delegation to `launch_app.main`. Keep CLI behavior and exit-code forwarding unchanged. Add a single startup log/print line stating that `run_app.py` is a compatibility wrapper and `launch_app.py` is canonical. Do not change command-line arguments behavior.</action>
  <acceptance_criteria>
    - platform_base/run_app.py contains `from launch_app import main as launch_main`
    - platform_base/run_app.py does not contain `platform_base.desktop.app`
    - running `cd platform_base && python run_app.py` exits through the same code path as `launch_app.py`
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python run_app.py</automated>
  </verify>
  <done>`run_app.py` no longer defines a separate desktop runtime startup path and clearly forwards to canonical launcher behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Mark canonical startup in launcher diagnostics</name>
  <files>platform_base/launch_app.py, platform_base/scripts/project_status.py</files>
  <read_first>
    - platform_base/launch_app.py
    - platform_base/scripts/project_status.py
    - platform_base/docs/USER_GUIDE.md
    - .planning/ROADMAP.md
  </read_first>
  <action>Add explicit canonical-startup metadata in `project_status.py` output (for example a line `Canonical launcher: launch_app.py`). Ensure `launch_app.py` startup output includes one stable marker line that can be reused by smoke scripts (`Platform Base v2.0 iniciado com sucesso!` already exists; keep it stable and documented in comments). Avoid changing runtime business logic in this task.</action>
  <acceptance_criteria>
    - platform_base/scripts/project_status.py contains `Canonical launcher: launch_app.py`
    - platform_base/launch_app.py still contains stable startup marker `Platform Base v2.0 iniciado com sucesso!`
    - `cd platform_base && python scripts/project_status.py` prints canonical launcher status line
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python scripts/project_status.py</automated>
  </verify>
  <done>Operational diagnostics consistently identify `launch_app.py` as the canonical entrypoint.</done>
</task>

</tasks>

<verification>
Run `python run_app.py` and `python scripts/project_status.py` from `platform_base/` and confirm only one canonical startup path is declared.
</verification>

<success_criteria>
UICO-02 is met for startup contract when `run_app.py` delegates to `launch_app.py` and diagnostics identify `launch_app.py` as canonical.
</success_criteria>

<output>
After completion, create `.planning/phases/02-consolidacao-do-runtime-de-ui/02-01-SUMMARY.md`
</output>
