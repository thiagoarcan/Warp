---
phase: 05-hardening-final-de-confiabilidade-e-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - platform_base/docs/reports/performance_baseline.json
  - platform_base/docs/reports/performance_baseline.md
  - platform_base/docs/reports/HARDENING_FINAL_REPORT.md
autonomous: true
requirements: [PERF-01, PERF-02]
must_haves:
  truths:
    - "Startup permanece dentro do limite aceito frente ao baseline"
    - "Memória permanece dentro do limite aceito frente ao baseline"
  artifacts:
    - path: "platform_base/docs/reports/performance_baseline.json"
      provides: "Métricas de startup e memória"
    - path: "platform_base/docs/reports/HARDENING_FINAL_REPORT.md"
      provides: "Conclusão final de não-regressão"
  key_links:
    - from: "platform_base/scripts/capture_runtime_baseline.py"
      to: "platform_base/docs/reports/performance_baseline.json"
      via: "captura de baseline"
      pattern: "output-json"
---

<objective>
Concluir hardening de performance com baseline atualizado e relatório explícito de não-regressão.

Purpose: Garantir estabilidade de startup/memória para evolução segura.
Output: Baseline atualizado e relatório final consolidado.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Capturar baseline de runtime no launcher canônico</name>
  <files>platform_base/docs/reports/performance_baseline.json</files>
  <action>Executar `python scripts/capture_runtime_baseline.py --launcher launch_app.py --output-json docs/reports/performance_baseline.json --timeout 60`.</action>
  <verify>
    <automated>cd platform_base && python scripts/capture_runtime_baseline.py --launcher launch_app.py --output-json docs/reports/performance_baseline.json --timeout 60</automated>
  </verify>
  <done>Arquivo de baseline atualizado com startup_detected, startup_seconds e peak_rss_mb.</done>
</task>

<task type="auto">
  <name>Task 2: Gerar relatório final de hardening</name>
  <files>platform_base/docs/reports/HARDENING_FINAL_REPORT.md</files>
  <action>Consolidar resultados de testes e performance em relatório final com conclusão explícita sobre não-regressão.</action>
  <verify>
    <automated>cd platform_base && python -c "from pathlib import Path; p=Path('docs/reports/HARDENING_FINAL_REPORT.md'); print('ok' if p.exists() else 'missing')"</automated>
  </verify>
  <done>Relatório final criado e alinhado aos critérios PERF-01/PERF-02.</done>
</task>
</tasks>

<success_criteria>
- Indicadores de startup e memória estão dentro dos limites aceitos frente ao baseline.
- Relatório final confirma aptidão para continuidade.
</success_criteria>
