---
phase: 03-consolidacao-de-modulos-canonicos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - platform_base/src/platform_base/core/session_state.py
  - platform_base/src/platform_base/core/signal_hub.py
  - platform_base/src/platform_base/desktop/session_state.py
  - platform_base/src/platform_base/desktop/signal_hub.py
autonomous: true
requirements:
  - DEDU-02
must_haves:
  truths:
    - Session and signal ownership has a single source of truth in core modules.
    - Desktop modules still import without runtime break by acting as compatibility wrappers.
  artifacts:
    - path: platform_base/src/platform_base/core/session_state.py
      provides: canonical SessionState implementation
    - path: platform_base/src/platform_base/core/signal_hub.py
      provides: canonical SignalHub implementation
    - path: platform_base/src/platform_base/desktop/session_state.py
      provides: compatibility re-export from core
    - path: platform_base/src/platform_base/desktop/signal_hub.py
      provides: compatibility re-export from core
  key_links:
    - from: platform_base/src/platform_base/desktop/session_state.py
      to: platform_base/src/platform_base/core/session_state.py
      via: import re-export
      pattern: from platform_base.core.session_state import
    - from: platform_base/src/platform_base/desktop/signal_hub.py
      to: platform_base/src/platform_base/core/signal_hub.py
      via: import re-export
      pattern: from platform_base.core.signal_hub import SignalHub
---

<objective>
Make `core` the canonical owner of session and signal state while preserving compatibility in `desktop`.

Purpose: Fulfill DEDU-02 by removing operational ownership duplication.
Output: core implementations as source-of-truth plus desktop wrappers.
</objective>

<execution_context>
@c:/Users/tdyb/.copilot/get-shit-done/workflows/execute-plan.md
@c:/Users/tdyb/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-consolidacao-de-modulos-canonicos/03-RESEARCH.md
@platform_base/src/platform_base/core/session_state.py
@platform_base/src/platform_base/core/signal_hub.py
@platform_base/src/platform_base/desktop/session_state.py
@platform_base/src/platform_base/desktop/signal_hub.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote SessionState canonical implementation to core</name>
  <files>platform_base/src/platform_base/core/session_state.py, platform_base/src/platform_base/desktop/session_state.py</files>
  <read_first>
    - platform_base/src/platform_base/core/session_state.py
    - platform_base/src/platform_base/desktop/session_state.py
    - platform_base/src/platform_base/ui/main_window_unified.py
    - platform_base/launch_app.py
    - platform_base/tests/automated/conftest.py
  </read_first>
  <action>Move the full SessionState implementation (dataclasses, signals, methods, constructor signature `SessionState(dataset_store, parent=None)`) into `core/session_state.py`. Then replace `desktop/session_state.py` with a compatibility re-export module importing `ProcessingState`, `SelectionState`, `SessionState`, `StreamingState`, `UIState`, and `ViewState` from `platform_base.core.session_state`. Keep names and constructor behavior unchanged.</action>
  <acceptance_criteria>
    - platform_base/src/platform_base/core/session_state.py contains `class SessionState(QObject)`
    - platform_base/src/platform_base/core/session_state.py does not contain re-export-only content
    - platform_base/src/platform_base/desktop/session_state.py contains `from platform_base.core.session_state import`
    - platform_base/src/platform_base/desktop/session_state.py does not contain a second `class SessionState(` declaration
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python -m pytest tests/automated/test_05_initialization.py -q --tb=short</automated>
  </verify>
  <done>SessionState ownership is centralized in core and desktop remains API-compatible.</done>
</task>

<task type="auto">
  <name>Task 2: Promote SignalHub canonical implementation to core</name>
  <files>platform_base/src/platform_base/core/signal_hub.py, platform_base/src/platform_base/desktop/signal_hub.py</files>
  <read_first>
    - platform_base/src/platform_base/core/signal_hub.py
    - platform_base/src/platform_base/desktop/signal_hub.py
    - platform_base/src/platform_base/ui/main_window_unified.py
    - platform_base/tests/automated/conftest.py
    - platform_base/tests/fixtures/qt_fixtures.py
  </read_first>
  <action>Move the full SignalHub implementation (signal declarations, emit helpers, stats method) into `core/signal_hub.py`. Then replace `desktop/signal_hub.py` with a compatibility re-export module importing `SignalHub` from `platform_base.core.signal_hub`. Keep signal names and method signatures unchanged so existing imports continue working.</action>
  <acceptance_criteria>
    - platform_base/src/platform_base/core/signal_hub.py contains `class SignalHub(QObject)`
    - platform_base/src/platform_base/desktop/signal_hub.py contains `from platform_base.core.signal_hub import SignalHub`
    - platform_base/src/platform_base/desktop/signal_hub.py does not contain a second `class SignalHub(` declaration
  </acceptance_criteria>
  <verify>
    <automated>cd platform_base &amp;&amp; python -m pytest tests/automated/test_04_signals_slots.py tests/automated/test_05_initialization.py -q --tb=short</automated>
  </verify>
  <done>SignalHub ownership is centralized in core and desktop remains API-compatible.</done>
</task>

</tasks>

<verification>
Run initialization and signals tests to ensure core ownership changes preserve runtime behavior.
</verification>

<success_criteria>
DEDU-02 is satisfied when SessionState and SignalHub have one canonical implementation in core and desktop modules are wrappers only.
</success_criteria>

<output>
After completion, create `.planning/phases/03-consolidacao-de-modulos-canonicos/03-01-SUMMARY.md`
</output>
